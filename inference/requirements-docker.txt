# 推理端容器依赖（Linux/CPU）。版本与 pyproject.toml 保持一致；
# 与本地 .venv 的差别仅在于 paddlepaddle 用 CPU 构建、不需要 nvidia-* 运行库。
paddlepaddle==3.3.0
numpy<2
opencv-python==4.6.0.66
onnxruntime>=1.23,<2
hyperlpr3==0.1.3
# Paddle 3.3.0 imports setuptools but does not declare it itself.
setuptools
